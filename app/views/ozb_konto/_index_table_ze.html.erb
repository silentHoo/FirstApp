<!-- :ktoNr, :eeKtoNr,:zeNr, :zeAbDatum, :zeEndDatum, :zeBetrag, 
     :laufzeit, :zahlModus, :tilgRate, :ansparRate, :kduRate, :rduRate, :zeStatus -->

<h3>ZE-Konten</h3>

<table class="record_listing">
  <tr>
    <th><%= OzbKonto.human_attribute_name(:ktoNr) %></th>
    <th><%= ZeKonto.human_attribute_name(:eeKtoNr) %></th>
    <th><%= ZeKonto.human_attribute_name(:zeNr) %></th>
    <th><%= ZeKonto.human_attribute_name(:zeAbDatum) %></th>
    <th><%= ZeKonto.human_attribute_name(:zeEndDatum) %></th>
    <th><%= ZeKonto.human_attribute_name(:zeBetrag) %></th>
    <th><%= ZeKonto.human_attribute_name(:laufzeit) %></th>
    <th><%= ZeKonto.human_attribute_name(:tilgrate) %></th>
    <th><%= ZeKonto.human_attribute_name(:ansparRate) %></th>
    <th colspan="3">Optionen</th>
  </tr>

  <% if @ze_konten.nil? %>
	<tr>
	  <td colspan="12" class="record_listing_empty">Es sind noch keine ZE-Konten vorhanden.</td>
	</tr>
  <% end %>
  
  <% if !@ze_konten.nil? %>
	  <% @ze_konten.each do |ze_konto| %>
	      <tr>
	        <td><%= ze_konto.ktoNr %></td>
	        <td><%= ze_konto.eeKtoNr %></td>
	        <td><%= ze_konto.zeNr %></td>
	        <td><%= ze_konto.zeAbDatum %></td>
	        <td><%= ze_konto.zeEndDatum %></td>
	        <td><%= ze_konto.zeBetrag %></td>
	        <td><%= ze_konto.laufzeit %></td>
	        <td><%= ze_konto.tilgRate %></td>
	        <td><%= ze_konto.ansparRate %></td>
	      
		    <% kkl_verlauf_link = "/OZBPerson/" + params[:mnr] + "/OZBKonto/ZE/" + ze_konto.ktoNr.to_s + "/verlauf" %>
	        <td class="table_option_column"><%= link_to "editieren", "/OZBPerson/" + params[:mnr] + "/OZBKonto/ZE/" + ze_konto.ktoNr.to_s + "/edit", :class => "edit_link" %></td>
	        <td class="table_option_column"><%= link_to "KKL-Verlauf", kkl_verlauf_link, :class => "konto_link" %></td>
	        <td class="table_option_column"><%= link_to "löschen", "/OZBPerson/" + params[:mnr] + "/OZBKonto/ZE/" + ze_konto.ktoNr.to_s + "/delete", :class => "delete_link", :confirm => "Soll das Konto " + ze_konto[:ktoNr].to_s + " wirklich gelöscht werden?" %></td>
	      </tr>
	  <% end %>
  <% end %>
</table>
<br />

<table class="actions">
  <tr>
    <td><%= link_to "Zurück", "/Admin", :class => "cancel_link" %></td>
    <% if @ze_konten.count < 4 then %>
      <td><%= link_to "Neues ZE-Konto hinzufügen", "/OZBPerson/" + params[:mnr] + "/OZBKonto/ZE/new", :class => "submit_ok" %></td>
    <% end %>
  </tr>
</table>